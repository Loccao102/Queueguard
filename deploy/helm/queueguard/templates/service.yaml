apiVersion: v1
kind: Service
metadata:
  name: {{ include "queueguard.fullname" . }}
  labels:
    {{- include "queueguard.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "queueguard.selectorLabels" . | nindent 4 }}
